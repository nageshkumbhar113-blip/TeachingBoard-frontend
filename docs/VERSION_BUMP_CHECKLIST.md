# 🔢 Version Bump Checklist — कोणत्या files मध्ये version बदलायचं

> पुढच्या release ला फक्त ही file उघडा, खालचे ६ ठिकाणं बदला. Search करायची गरज नाही.

**सध्याची version (last updated 2026-09-06):** `7.4.45` · versionCode `142` · SW `v142`

---

## ✋ MANUAL — हे ५ ठिकाणं स्वतः बदलायचे

| # | File | Line | काय बदलायचं | उदाहरण |
|---|------|------|-------------|--------|
| 1 | `env.js` | 2 | `window.APP_VERSION = 'X.X.X'` | `'7.0.0'` |
| 2 | `package.json` | 3 | `"version": "X.X.X"` | `"7.0.0"` |
| 3 | `build-student.bat` | 7–8 | `set VERSION=X.X.X` **+** `set VERSION_CODE=XX` | `7.0.0` / `70` |
| 4 | `build-admin.bat` | 7–8 | `set VERSION=X.X.X` **+** `set VERSION_CODE=XX` | `7.0.0` / `70` |
| 5 | `sw.js` | 1 | `const SW_VERSION = 'vXX'` (**+1**) | `v44` → `v45` |

> ⚠️ दोन्ही `.bat` files मध्ये VERSION **same** ठेवा.

---

## 🤖 AUTO-PATCHED — यांना हात लावू नका

`build-student.bat` / `build-admin.bat` चालवल्यावर हे आपोआप patch होतात (VERSION + VERSION_CODE मधून):

| File | काय auto होतं | Note |
|------|---------------|------|
| `android/app/build.gradle` | `versionCode` + `versionName` | **gitignored** — manually edit/commit करू नका |
| `env.js` (APP_VERSION) | bat पुन्हा patch करते | पण #1 मध्ये manually पण ठेवा (web deploy साठी) |

> ⚠️ **APK build करण्याआधी `build-student.bat` चालवणं mandatory** — नाहीतर `build.gradle` जुन्या version वर राहील.

---

## 🚫 VERSION नसतं — इथे काही बदलायचं नाही

- `capacitor.config.ts`, `capacitor-student.config.ts`, `capacitor-admin.config.ts` → फक्त `appId` / `appName` (version नाही)
- `TeachingBoard-backend/package.json` → backend version (`1.0.0`), user-facing नाही, बदलायची गरज नाही

---

## 📏 नियम

- **versionCode** दर release ला **वाढलाच पाहिजे** (Android install साठी). Pattern: major × 10 → `5.0.x=51`, `6.0.0=60`, `7.0.0=70`.
- **SW_VERSION** — कोणताही JS/CSS/HTML बदलला तर **+1** mandatory (नाहीतर users ला जुने cached files मिळतात).
- **Semver:** bug fix → `x.x.+1` · नवीन feature → `x.+1.0` · मोठा redesign → `+1.0.0`

---

## 🚀 Release order (थोडक्यात)

1. Code changes + verify
2. वरचे ६ ठिकाणं बदला (ही file बघून)
3. `git commit` + `git push` (frontend **आणि** backend दोन्ही)
4. Backend changes असतील → Render auto-deploy होतो (env vars set आहेत का बघा)
5. `build-student.bat` चालवा → Android Studio → Signed APK
6. `build-admin.bat` (फक्त `admin-app/`, `core/`, `sw.js` बदलले असतील तर)
7. GitHub Release: tag `vX.X.X` + APK upload
8. Admin app → App Updates → GitHub वरून Fetch → Save

---

## 📜 Version इतिहास

| Version | versionCode | SW | तारीख | काय |
|---------|-------------|----|-------|-----|
| 5.0.1 | 51 | v43 | 2026-06-29 | Login bug fixes |
| 6.0.0 | 60 | v43 | — | (bat मध्ये set, build न करता) |
| **7.0.0** | **70** | **v44** | **2026-06-30** | Razorpay subscriptions, self-registration, SLS fixes |
| ... | ... | ... | ... | (history gap — table not kept current between 7.0.0 and 7.3.0) |
| **7.4.0** | **97** | **v97** | **2026-08-13** | MCQ Mixed Test Paper Builder + Paper Pattern, YouTube Teacher Partner Portal, Android TV remote support, --text1 CSS contrast fix |
| **7.4.1** | **98** | **v98** | **2026-08-13** | Live-testing fixes: random-pick order shuffle, 4 explicit Paper Modes (Regular/Whole Chapter/Whole Subject/Paper Pattern), labeled section fields, AI bulk-paste prompt, visible Pattern Name field |
| **7.4.2** | **99** | **v99** | **2026-08-13** | Mixed-test student visibility fix, Subject-optional for Paper Pattern, Exam Instructions gate + AI pattern import, PDF export margin fix + realistic layout (Roll No/Seat No box, 2-column, question/option images) |
| **7.4.3** | **100** | **v100** | **2026-08-15** | Exercise Manager Preview/Publish/PDF + optional diagram upload, Notes PDF export, markdown table support, new 📚 Books tab (Notes/Exercise/Subject-wise/Chapter-wise/Paper Pattern Books with hard page-break-per-section + printed-workbook style) |
| **7.4.4** | **101** | **v101** | **2026-08-15** | Notes Book fix: Revision-Box-only / Exam-Tags-only notes were silently dropped as "empty" (real bug found live testing 8th NMMS batch) |
| **7.4.5** | **102** | **v102** | **2026-08-15** | Notes Book follow-up: a bare examTags tag alone no longer counts as "content" — was letting title-only stub notes through as a practically-blank PDF (real bug, found from an actual generated PDF) |
| **7.4.6** | **103** | **v103** | **2026-08-15** | Books real root-cause fix: Notes Book was reading the concept LIST endpoint (title/tags only, backend deliberately strips content) instead of fetching each concept's full detail — real content was never being read at all. Also fixed silent 20-item pagination truncation on the Exercise questions endpoint (Books + Exercise Manager) |
| **7.4.7** | **104** | **v104** | **2026-08-16** | Notes Book: fixed half-empty pages — dropped multi-column note-body layout (both CSS auto-balance and a fixed grid left visible empty space with real, unevenly-sized content); single column now, no gaps |
| **7.4.8** | **105** | **v105** | **2026-08-16** | PDF exports (Exercise/Quiz/Notes) now render KaTeX math instead of printing raw $...$ LaTeX text — real bug found from an actual exported PDF ("acceleration of $2,m/s^2$" printed literally) |
| **7.4.9** | **106** | **v106** | **2026-08-16** | YouTube Teacher Portal: real embedded video preview on both admin Approvals (was plain text link, admin couldn't see the video before approving) and teacher's Add Exercise Video screen (was no preview at all before submitting) |
| **7.4.10** | **107** | **v107** | **2026-08-16** | YouTube Teacher Portal "My Videos": added Edit button (was missing entirely — approved videos couldn't be edited at all). Reuses existing backend live/pending-edit pattern — editing an approved video locks Batch/Subject/Chapter/Exercise/Part (identity), lets teacher change only the link, resubmits to pending, and the old approved video stays visible to students until the edit is re-approved |
| **7.4.11** | **108** | **v108** | **2026-08-16** | Admin "YouTube Teachers → Video Gaps" Subject dropdown was rendering blank/empty options — real bug found live: it read subjects as `{name}` objects but `GET /batches` returns them as plain strings; fixed the mapping |
| **7.4.12** | **109** | **v109** | **2026-08-16** | YouTube Teacher videos now support Notes Concepts, not just Exercises: Add Video form gets an Exercise/Concept toggle; My Teaching Areas gets a "View Content" list (every Exercise+Concept in a Subject, ✅ marks what you've covered, tap to add/edit); Notes concept screen gets the same "🎬 N Videos Available" button exercises already have; Admin Approvals shows Concept videos correctly |
| **7.4.13** | **110** | **v110** | **2026-08-16** | YouTube Teacher plan pricing is now admin-editable (Admin → YouTube Teachers → ⚙️ Plan Pricing) — was hardcoded in backend code with no admin UI at all; Teacher Portal landing page + plan-select screen now show the live admin-configured prices instead of static ₹499/₹3999/₹199/3-day text |
| **7.4.14** | **111** | **v111** | **2026-08-17** | Exercise question/answer labels now match the actual content language instead of always Marathi — real bug found live: an English-only question printed with a Marathi "प्रश्न N." label; now "Q N."/"Answer" for English content, "प्रश्न N."/"उत्तर" for Marathi, in both the Exercise PDF export and admin Exercise Manager's question lists |
| **7.4.15** | **112** | **v112** | **2026-09-02** | Admin Classes: Subject and Chapter names had no Edit button (only add/delete) — added Edit everywhere, backed by new rename endpoints that cascade the name into every collection that stores it, including the composite chapterId (SLSQuestion/Concept) so existing Notes/Exercises stay linked instead of orphaning |
| **7.4.16** | **113** | **v113** | **2026-09-02** | Student Notes: added a "Next →" button beside "← Back" on a concept — moves to the next concept in the chapter's list directly (reuses viewConcept), only shown when one exists |
| **7.4.17** | **114** | **v114** | **2026-09-02** | Student bottom nav: moved Notes out from under Words (was vocab-tab-notes, nested inside the Words screen's own tab bar) to its own "Notes" tab in the main bottom navigation, alongside Home/Stats/Board/Words/Exercise/Me |
| **7.4.18** | **115** | **v115** | **2026-09-02** | Home search (new): one search bar on Home finds both Notes concepts and Exercise questions together — new backend text-search endpoint for Exercise questions (Notes/concept search already existed), new homeSearch.js combining both, clicking a result jumps straight to that concept or scrolls to + highlights that exact question |
| **7.4.19** | **116** | **v116** | **2026-09-03** | Real bug found live: deleted "Std N" demo batches kept coming back. `initDefaultBatches()` was reseeding 6 hardcoded demo batches (Std 5-10) on any fresh/cleared local storage, guarded by a flag stored in that SAME storage — cache-clear/reinstall wiped the guard too, so it looked like "never seeded" and reseeded. App has real admin-managed batches now; demo seeding permanently disabled (no-op) |
| **7.4.20** | **117** | **v117** | **2026-09-03** | Admin Concepts (SLS): new "📚 Auto-fill Lesson (सगळे Concepts)" — paste a whole multi-concept lesson file (split on "# Concept N: Title" headings) and it creates every concept as a Draft in one go, reusing the existing single-concept parser per chunk + existing-title dedup. Existing single-concept "✨ Auto-fill करा" untouched |
| **7.4.21** | **118** | **v118** | **2026-09-03** | Admin Concepts (SLS) editor had no way back to the list without scrolling to a buried "Cancel" button — added a "← Back to list" + "Next: <title> →" bar at the top of the editor (Next moves to the following concept in the current chapter's list — useful for reviewing/publishing a batch just created via Bulk Lesson Import one after another) |
| **7.4.22** | **119** | **v119** | **2026-09-03** | Real bug found live right after 7.4.21 shipped: Publish/Save was force-closing the concept editor back to the list (it reused `_onChapterChange`, which was built for actual chapter-dropdown switches and unconditionally hides the editor), defeating the "Next" button added in 7.4.21. Extracted a non-disruptive `_refreshConceptsList()` so Publish/Save now reloads the list in the background but keeps the editor open — Next can now actually be used to review a batch of concepts one after another (e.g. after Bulk Lesson Import) |
| **7.4.23** | **120** | **v120** | **2026-09-03** | Real bug found live: a newly-added Exercise (e.g. Mathematics I → Linear Equations in Two Variables) never showed up in the student app — "Select Subject" rendered completely empty. Root cause: the student app's local Subject/Chapter dropdowns were built only from `DB.syncHierarchyFromExisting()`, which reconstructs the hierarchy purely from locally-cached legacy MCQ Question/Quiz data — a subject/chapter with only Notes/Exercise content (no MCQ quiz ever cached) never made it in. `GET /batches` already has the real catalog but is teacher/admin-only and includes pricing. Added a new student-safe `GET /batches/student/hierarchy` (own assigned batches only, no pricing) + a matching client-side sync, called at Home load/refresh alongside the existing local repair pass |
| **7.4.24** | **121** | **v121** | **2026-09-03** | Student Notes had no Subject selection — every subject's chapters rendered together in one mixed grid (Std 9/10, Science/Maths all combined, no way to narrow down). Added a Subjects grid as the new top level (grouped by batch+subject, so a same-named subject in two different batches — e.g. two different "Science I"s — stays correctly separated), then that subject's Chapters, then Concepts as before. Back/title/search-clear all updated to match the new level. Verified with a Playwright harness against the real module, including the same-subject-different-batch isolation case |
| **7.4.25** | **122** | **v122** | **2026-09-03** | Admin Exercise Manager: real bug found live — deleting several questions then re-adding the same ones via Auto-fill wrongly reported them all as "already existed" (duplicates skipped), even though the deletes had genuinely gone through on the server. Root cause: each Delete click's list-refresh is an independent fetch, and an earlier click's slower response could resolve AFTER a later click's, silently overwriting the current (correct) in-memory list with stale (pre-delete) data — a classic out-of-order-response race, more likely the more questions were deleted in quick succession. Fixed with a monotonic request-token guard so only the most-recently-started refresh's response is ever applied. Verified with a Playwright test that deterministically reproduces the exact race (an engineered stale-but-slow response vs a fresh-but-fast one) and confirms the fresh one always wins |
| **7.4.26** | **123** | **v123** | **2026-09-03** | Admin Exercise Manager: new questions (Manual Save + Auto-fill) were saved with `status:'published'` — live to students immediately, before the "Publish All" button was ever clicked. User confirmed this wasn't intended (asked directly, given it changes when students see new content) — changed both creation paths to `status:'draft'`, matching the "Publish All" button's own implied workflow and the same draft-first pattern already used by Concepts/Notes. Verified with a Playwright test confirming both paths now create as draft |
| **7.4.27** | **124** | **v124** | **2026-09-03** | Admin Exercise Manager: added "🗑 Delete Exercise" — deletes every question under the currently-selected Exercise No. in one go (confirm dialog shows the count first), instead of having to delete each question one at a time. Loops the existing single-question delete endpoint (no new bulk-delete API), then refreshes the Exercise No. chip list so the emptied one drops out on its own. New `.btn-danger` style added (matches the existing lightweight `.btn`/`.btn-small` system used by its sibling Preview/PDF buttons). Verified with a Playwright test: declining the confirm deletes nothing; confirming deletes only the selected Exercise No.'s questions and leaves a sibling Exercise No. untouched |
| **7.4.28** | **125** | **v125** | **2026-09-03** | Student app: real gap found — the app never called `navigator.storage.persist()`, so Android/Chrome could opportunistically evict the offline Notes/Exercise IndexedDB cache under storage pressure, most noticeable after fully closing and reopening the app ("cached content works once, then disappears"). Now requested once at startup, best-effort/fire-and-forget — degrades the same as before if unsupported or denied |
| **7.4.29** | **126** | **v126** | **2026-09-03** | Student app: added a one-time background "full offline sync" — right after login/home-load, every Note's full content + every Exercise question for the student's own batch(es) downloads and caches in one background pass (non-blocking, student keeps using the app meanwhile), instead of needing to open each one individually while online first. Backed by a new single-request backend endpoint (`GET /sls/full-sync`, own commit in TeachingBoard-backend) rather than one request per chapter. Runs once per assigned-batch set (tracked locally), safely re-attempts later if a previous try never completed (e.g. no internet at the time). Verified with a Playwright test: no-batches no-ops, first run caches everything and sets the flag, a repeat run with the same batches is skipped, a changed batch set re-runs |
| **7.4.30** | **127** | **v127** | **2026-09-03** | Student app, two follow-ups: (1) the one-time full offline sync now shows a visible "नवीन माहिती Update होत आहे…" animated overlay at login, only for the genuine first run (an already-synced return visit shows nothing — no flash); (2) real gap fixed — expiry was only ever enforced from an actual server response, so a student whose cached `expiry_date` had already passed could keep using a fully offline-cached app indefinitely just by staying offline. Added a local, deliberately non-destructive expiry gate (same date-boundary rule as the backend's `isExpiredDate()`) that pauses access when offline past the cached expiry — doesn't touch token/profile, so a wrong device clock or an already-renewed-but-not-yet-synced student isn't wrongly logged out; the moment they're back online the existing authoritative server check takes over (confirms renewed -> unlocks, or confirms expired -> the real `markExpired()`/renewal flow). Verified with a Playwright test covering the date-boundary logic and all three retry outcomes (renewed, still-expired, network failure) plus both overlay states (shown-then-hidden vs. never-shown-when-already-synced) |
| **7.4.31** | **128** | **v128** | **2026-09-03** | Follow-up to 7.4.30's login overlay: it only wrapped the offline-sync step, but the actual slow part on a cold Render free/starter-tier backend is the profile-refresh call (`fetchStudentMe()`) right before it — that gap showed nothing between PIN unlock and home appearing. Merged both steps under one continuous overlay (`_completeStudentLogin`, replacing the old inline post-PIN-unlock block) shown unconditionally the instant PIN unlock succeeds; on a warm server with already-synced content it resolves in well under a second. Verified with a Playwright test: fast/no-sync path never gets stuck shown, success+sync-needed path stays up through the sync with updating text, a server-confirmed-expired response hides it cleanly, and a network failure combined with a locally-expired cached profile hides the sync overlay before showing the offline-expired gate (no stacking) |
| **7.4.32** | **129** | **v129** | **2026-09-04** | Admin Classes: new "Board → Medium → Class" structured batch creation, replacing the old single free-text prompt that was the literal root cause of the inconsistent/typo'd batch names found live this session. New optional Board/Medium/Class metadata fields on Batch (backend: `TeachingBoard-backend`), purely descriptive — the batch `name` string stays the one identity key everywhere, unchanged. New "🏷️ Details" action lets an EXISTING batch get this metadata backfilled with its name field locked read-only — deliberately can never trigger a rename (which cascades chapterId across every Note/Exercise). Verified with a Playwright test including the critical safety assertion: editing an existing batch's details always saves with the exact same old/new name, and never calls the create-batch path. Phase 1 of a larger plan — Phases 2-3 (Board/Medium search filters on the student subscribe picker and YouTube Teacher Teaching Areas) follow next |
| **7.4.33** | **130** | **v130** | **2026-09-04** | Phases 2-3 of the Board/Medium/Class plan: added a Board/Medium search filter above the batch carousel on the student "Choose Plan" subscribe screen (`payment.js`), and the same filter above the batch grid on YouTube Teacher "My Teaching Areas" (`teacher-portal.js`) — both only render when there's genuinely more than one distinct value to filter by, so a small catalog stays uncluttered, and both leave every existing click/swipe/toggle handler untouched. Verified with Playwright tests on both: filter options match the real distinct board/medium values, filtering narrows correctly (including a no-match combo showing a clean empty state on the student side), a single-board-and-medium catalog shows no filter UI at all, and legacy batches with no board/medium metadata are entirely unaffected |
| **7.4.34** | **131** | **v131** | **2026-09-04** | Four real bugs + one feature found live in one session: (1) opening a Notes chapter's list while online silently overwrote already-fully-cached Notes back to a content-less "list" shape (same `sls_concepts` row, keyed by `_id`, gets replaced) — made "Note works offline once, then stops" trivially reproducible by just browsing the list again; fixed `DB.saveConceptsCache` to never downgrade a concept that's already fully cached; (2) Notes never called `MATH.renderElement()` at all, so `$$...$$` formulas showed as raw LaTeX text instead of rendering — added the same call Exercise/Word Tests already had; (3) editing a Batch/Subject/Chapter name could leave the OLD name behind as a duplicate while the rename otherwise worked — root cause: local state was optimistically renamed BEFORE the (slower, especially for Batch) server call finished, and a background sync landing in that window would resurrect the old name since it no longer matched anything locally; reordered all three rename handlers to call the server first and only touch local state once it's confirmed done; (4) enabled Android WebView pinch-to-zoom (`capacitor-student.config.ts`, `android.zoomEnabled` — off by default in Capacitor, no way to scope it to just one screen) for reading dense Notes text/diagrams up close. Verified with Playwright tests: the cache-downgrade fix (full content survives a list-refresh, normal never-opened concepts unaffected), the MATH-render call firing on every rendered note, and the rename race fix (local state never touched when the server call fails; server always called before local update when it succeeds) |
| **7.4.35** | **132** | **v132** | **2026-09-05** | Two changes: (1) new "📤 मित्रांना App Share करा" button on the student Home screen (`_shareApp()`) — Capacitor `Share` plugin → Web Share API → clipboard-copy fallback chain, same pattern already proven in `core/fileExport.js`'s PDF share; (2) real bug found live — the topbar's `#btn-home`/`#btn-profile` duplicated the bottom-nav's Home/Me tabs on every normal screen (screenshot showed both "Home" labels stacked). They're only the actual navigation when bottom-nav itself is hidden (Teacher/Parent dashboard, quiz/test-player — confirmed pre-existing intent via `testPlayer.js`'s own mid-test `#btn-home` cleanup handler). Added a single `_setBottomNavVisible()` helper used at all 3 existing bottom-nav toggle sites so the topbar buttons now mirror bottom-nav's own visibility — hidden whenever bottom-nav is shown, shown as the fallback nav whenever it's hidden. Verified with a Playwright test against the real `app.js` covering all 3 toggle sites (home/quiz/test-player via `showScreen`, plus Teacher and Parent dashboard) and the helper directly |
| **7.4.36** | **133** | **v133** | **2026-09-05** | New feature: Home Banners — an admin-managed promo carousel above the search bar (design mockup approved first, per the user's own request, before any code). Backend (own commit in `TeachingBoard-backend`): new `Banner` model + admin CRUD (`/api/admin/banners`) + student feed (`GET /api/banners/for-student`, scoped to the student's own assigned batch(es) plus every 'all'-scope banner, never trusting a client-supplied batch) + an `/open` engagement-counter endpoint. Student app: new carousel section (`#home-banners`) with auto ping-pong scroll (advances forward, reverses at the last slide, loops — matches the approved mockup), swipe/drag support that pauses autoplay briefly, tap-to-navigate (jumps to a batch by clicking its existing `.batch-card`, or opens an external URL via the `@capacitor/browser` plugin with a `window.open` fallback — same pattern as `payment.js`'s Razorpay checkout), cyclic fallback gradients when a banner has no image, cache-first offline rendering via `DB.getSetting`/`setSetting` (reuses the existing generic `settings` store — no IndexedDB schema/version bump needed), and complete HTML-escaping of admin-entered title/subtitle text. Entirely hidden when a student has zero banners — no empty box, no regression for any existing batch. Admin UI to actually create/manage banners is a deliberately separate, later phase (this ships the student-facing half + full backend first). Verified: 18 backend checks against the live shared MongoDB (real admin + real student with real assigned_batches — scoping, validation, auth, response never leaks admin-only fields, openCount increments, deactivate takes effect immediately), plus 14 Playwright checks against the real `app.js` (empty/1/3-banner rendering, dot visibility, gradient cycling, ping-pong stepping sequence, batch/url/none tap behavior, Capacitor-vs-fallback branch, engagement counter fires for every tap, XSS-safe escaping, image vs gradient background, and the cache-first/offline/network-failure paths of `_loadHomeBanners`) |
| **7.4.37** | **134** | **v134** | **2026-09-05** | Admin UI for Home Banners (the deliberately-deferred second half of 7.4.36): new "🎯 Banners" tab in the existing sidebar + mobile drawer (`admin-app/bannerAdmin.js`, follows `batchPricingManager.js`'s exact IIFE/CRUD conventions — `_apiBase()`/`_authHeaders()`/`_esc()`), wired into `admin.js`'s existing generic `.atab` tab-switcher (one added line, no existing tab-switch logic touched). Create/edit form: title, optional subtitle + image URL, tap-action (none / jump to a Batch / open an external URL), scope (All Batches, or specific Batch(es) via the same `.student-batch-item` checkbox pattern already used for Student Access), order, active toggle. List view shows scope, active/off status, tap-action, order, and the open-count engagement metric per banner, with Edit/Delete actions (delete reuses the existing `APP.confirmAsync` pattern). Talks to the `/api/admin/banners` endpoints already shipped in 7.4.36's backend commit — no new API. Verified with 14 Playwright checks against the real `bannerAdmin.js` (batch-option population, empty-state, full card rendering incl. openCount, XSS-safe escaping of admin-entered text, link-type/scope field show-hide, all 3 validation rules, create/edit payload correctness, edit pre-fill including scope checkboxes, and delete confirm/decline) |
| **7.4.38** | **135** | **v135** | **2026-09-05** | User-reported correction: the Share App message (`_shareApp()`) called the app "मोफत अभ्यास App" (free study app) — incorrect once subscriptions launched, since only a trial is free and the actual study content is paid after that (matches `get-app.html`'s own accurate "Free Trial" framing, never "free app"). Removed the false claim; text now just describes the app without any pricing claim. Also live-verified the Home Banners feature end-to-end for the first time against real production: created a real banner via the live admin API (caught and fixed a genuine issue in the process — passing Devanagari/emoji text through a raw shell `curl -d` argument on Windows silently corrupted the UTF-8 bytes; fixed by sending the JSON body via `--data-binary @file` instead, confirmed the corrected banner reads back byte-correct from the live database) and confirmed via a real coordinate-based Playwright click (using the actual shipped CSS/JS, not a direct function call) that tapping a banner with a real Tap-action (Batch/URL) correctly navigates, with a screenshot confirming the banner renders correctly on Home. Also confirmed a `linkType: 'none'` banner intentionally does nothing on tap and that this is by design (an announcement-only banner), not a bug — relevant since a banner created without picking a Tap-action in the Admin form defaults to exactly this |
| **7.4.39** | **136** | **v136** | **2026-09-05** | User-requested redesign of Home's top: removed the old static "Select Class & Subject / Choose your batch to begin" hero text entirely (`.home-hero`/`.home-title`/`.home-sub` markup + CSS all removed — nothing else referenced them) and made the banner carousel taller (120px → 170px, bigger title, more padding) to occupy that same space as the new de-facto header. Also: the carousel is now **never empty** — when a student has zero admin banners (today's default, until Admin actually creates one), a rotating set of 8 built-in Marathi/English study-motivation quotes (`_buildDefaultBanners`) fills the space instead, starting from a randomized point each load so it's not always the same first line. These default quotes are purely decorative: tap does nothing and they're never counted in the openCount engagement metric (`_openBanner`'s `isDefault` guard) — the instant a real admin banner exists for a student, it fully replaces the defaults (never mixed). Cleaned up the earlier live test banner from production now that this ships, so real students immediately see the new default quotes instead. Verified with 8 Playwright checks against the real app.js + shipped CSS (structural check that the old hero markup is gone, default-quote generation and its rotation math, fallback substitution logic, real coordinate-click proving defaults are tap-inert while real banners still track normally) plus a visual screenshot of the taller hero-replacement layout |
| **7.4.40** | **137** | **v137** | **2026-09-05** | User-requested follow-up: the 8 fallback study-quotes from 7.4.39 were hardcoded in `app.js` — made them admin-editable instead. New `DefaultBannerQuote` model + admin CRUD (`/api/admin/banners/default-quotes`, own commit in `TeachingBoard-backend`) + student read endpoint (`/api/banners/default-quotes`, active-only, ordered, plain text array). New "💬 Default Quotes" section inside the existing Admin → 🎯 Banners tab (`bannerAdmin.js`) — add/edit via `APP.promptAsync` (matches the existing rename-prompt convention elsewhere in admin.js, no new modal needed for a single-field item), toggle active/off, delete. Student app (`_loadHomeBanners`) now fetches+caches admin-configured quotes the same cache-first way as real banners, feeding them into `_buildDefaultBanners` (now takes an optional quotes list); the original hardcoded 8 quotes remain as the ultimate fallback — used only when admin has configured zero custom quotes, so the "Home is never empty" guarantee holds even before any admin ever touches this screen. Verified: 14 backend checks against the live shared MongoDB (CRUD, ordering, active-only filtering for students, validation, auth, response shape), 6 Playwright checks on the student side (quotes-provided vs hardcoded-fallback branching, ignored once a real banner exists, cached separately and used correctly offline), and 9 Playwright checks on the admin side (empty state, card rendering, XSS-safe escaping, create/edit/toggle/delete all send the right request, no-op guards for empty/unchanged input) |
| **7.4.41** | **138** | **v138** | **2026-09-06** | User-reported: PDF downloads (Books, Paper Builder) still showed garbled math — fraction bars overlapping/striking through the numerator text — even though math was rendering fine in the live Notes viewer. Two distinct real bugs found across all 5 PDF-export pipelines (`core/notesPdf.js`, `exercisePdf.js`, `quizPdf.js` [×2 render paths — single item + the multi-item "Book" pagination `_renderGroupsToBlob` used by Books], `paperPdf.js`, `pdf.js`): (1) `paperPdf.js` (Paper Builder) never ran KaTeX at all — retrofitted the exact same `_ensureKatex`/`_renderMath` pipeline the other three already had, so `$...$` math no longer prints as literal dollar-sign text; (2) **the actual cause of the reported garbling** — even where KaTeX *did* run, `html2canvas` could still snapshot before KaTeX's own `@font-face` web fonts finished loading/painting, badly mis-measuring fraction/exponent layout (numerator+denominator collapsing onto one line with the fraction bar cutting through — exactly the reported look). Fixed in all 5 capture sites by forcing a reflow then awaiting `document.fonts.ready` before every `html2canvas()` call. Also retrofitted the older browser-print-based export (`core/pdf.js`, used for legacy MCQ Question/Quiz "print to PDF") with the same KaTeX rendering, run just before `window.print()` — real browser print has no font-load race (native rendering, not a canvas snapshot), so only the missing-KaTeX half of the fix applied there. Verified with 5 Playwright checks against the real files: confirmed exact call ordering (`renderMathInElement` → `fonts.ready` resolves → `html2canvas`, never reversed) in `paperPdf.js`, `notesPdf.js`, `exercisePdf.js`, and `quizPdf.js`'s Book-pagination path, plus confirmed `pdf.js`'s print window carries the KaTeX `<script>`/`<link>` tags and calls `renderMathInElement` before `win.print()` |
| **7.4.42** | **139** | **v139** | **2026-09-06** | Follow-up after the user re-tested 7.4.41 and still saw the fraction bar rendering as a strikethrough through the numerator (not below it) in a downloaded Book PDF. Added `foreignObjectRendering: true` to all 5 `html2canvas()` calls (`notesPdf.js`, `exercisePdf.js`, `quizPdf.js` ×2, `paperPdf.js`) — this makes html2canvas delegate actual pixel rendering to the browser's native SVG `<foreignObject>` engine instead of its own manual DOM-to-canvas re-implementation, which is a well-documented source of exactly this KaTeX fraction-bar mis-positioning (html2canvas's manual layout math can miscalculate KaTeX's precise vlist-based numerator/denominator stacking even once fonts are loaded, while the browser's real engine — the one already rendering KaTeX correctly on-screen — does not). **Honesty note:** despite trying multiple realistic reproductions (matching production's exact off-screen container position/width/line-height/font-stack) in Playwright/Chromium, the exact garbled render could not be reproduced locally to directly confirm this specific fix — `foreignObjectRendering` is shipped because it is the documented, targeted fix for this exact symptom and was verified to introduce no regression (a real KaTeX+html2canvas render was captured both ways and compared, both rendering identically-correct in every reproduction attempt), not because the bug was caught red-handed locally. Also flagged for the user: PDF export code lives in files bundled into the APK at build time, not fetched live from Vercel — a web-only deploy never reaches an already-installed APK, so confirming this fix requires either testing via the web app or installing a freshly built APK |
| **7.4.43** | **140** | **v140** | **2026-09-06** | Urgent revert of 7.4.42's `foreignObjectRendering: true` addition — the user gave the exact reproduction steps (Admin → Books → Notes Book → 9TH CLASS Semi &English medium → Science I → 1.laws of motion), which let this be tested against the REAL chapter content for the first time (fetched directly from production via the same API/chapterId the Books feature itself uses) instead of a synthetic sample. That direct reproduction found: (1) 7.4.41's fix alone (fonts.ready wait, no foreignObjectRendering) already renders every formula in this real chapter perfectly — "Speed = Total distance travelled⁄Time required," "v = u + at," "s = ut + ½at²," "v² = u² + 2as," all correctly stacked, confirming 7.4.41 was never actually broken; (2) 7.4.42's `foreignObjectRendering: true` addition was a real regression — it silently produces a **completely blank canvas** (confirmed: 190KB of blank white vs 1.7MB of real rendered content from the exact same input) for any container positioned at `left:-99999px`, which is how every one of these 5 capture sites positions its content. Reverted `foreignObjectRendering` from all 5 sites, keeping only 7.4.41's fonts.ready fix. Likely explanation for the user seeing the bug persist after 7.4.41 shipped: a stale cached bundle in the browser, not a real gap in that fix — asked the user to retest in a fresh/incognito window to rule out caching entirely. Lesson applied going forward: verify PDF-export fixes against real off-screen-positioned content (matching the actual capture technique), not an on-screen synthetic sample, before shipping |
| **7.4.44** | **141** | **v141** | **2026-09-06** | User-requested: Paper Builder (Admin) now supports a single Question Paper spanning **multiple Subjects and multiple Chapters**, not just one. Batch stays single-select; Subject and Chapter became checkbox multi-select lists (`.student-batch-item` pattern, matching Student Access/Banners) — Chapter list combines chapters from every checked Subject, each tagged with its own Subject once more than one is selected. Question search and Auto-fill now query all checked chapters together (own backend commit in `TeachingBoard-backend`: `getQuestions`'s `chapterId` accepts a comma-separated list → `$in` when more than one, every existing single-chapterId caller elsewhere unaffected). Backend paper-save (`createPaperManual`) accepts optional `chapterIds[]`/`subjectIds[]` alongside the existing singular fields (kept populated with the first selected one for backward compatibility); the isolation check now allows any of the selected chapters instead of exactly one; paper numbering stays per-chapter for an ordinary single-chapter paper, and counts per-batch for a genuinely multi-chapter one (confirmed with the user). PDF header now joins multiple subject names when present. Verified: 9 backend checks against the live shared MongoDB using real questions from two real chapters (multi-chapterId query, isolation rejection, multi- and single-chapter paper creation, unchanged shape for the old single-chapter path) and 10 Playwright checks on the admin frontend (checklist rendering across multiple subjects, chapter tagging, visibility toggling, comma-joined query param, per-question origin tag shown only when 2+ chapters are selected, save payload correctness for both multi and single-chapter cases, blocked-save validation) |
| **7.4.45** | **142** | **v142** | **2026-09-06** | User-reported: a downloaded Paper Builder PDF showed a "match the columns" question as literal `\| S. No. \| Column 1 \|...` pipe-text instead of a real table, and any attached question/answer diagrams never appeared at all. Root cause: `core/paperPdf.js` had none of the markdown-table/diagram rendering `notesPdf.js`/`exercisePdf.js` already had — it only ever did a plain HTML-escape on question/answer text. Ported the exact same `_richText`/`_mdTableToHtml` (pipe-table → real `<table>`, `**bold**` → `<strong>`) and diagram rendering (`questionDiagrams[]`/`answerDiagrams[]` → `<img>`, already present in the API response, just never rendered) from `exercisePdf.js`. No backend change needed — the diagram data was already being sent. Verified with 5 Playwright checks (table rendering, diagrams as images, math+table together, bold markdown, plain text unaffected) plus a direct reproduction using the user's own exact reported question (fetched live from production) — screenshotted the actual rendered output: a clean 3-column, 3-row table |
